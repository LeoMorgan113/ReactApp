
let initialState = {
    friends: [
        {id: 1, name: 'Lesha'},
        {id: 2, name: 'Anna'},
        {id: 3, name: 'Nikita'}
    ]
};

const sidebarReducer = (state = initialState, action) => {
    return state;
}
export default sidebarReducer;